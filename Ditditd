-- =============================================
-- PANEL TÉLÉPORTATION JOUEURS - Delta Executor
-- =============================================

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local selectedPlayer = nil

-- Création du ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "TpPanelDelta"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 420, 0, 500)
MainFrame.Position = UDim2.new(0.5, -210, 0.5, -250)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundColor3 = Color3.fromRGB(0, 255, 136)
Title.Text = "Panel Téléportation"
Title.TextColor3 = Color3.new(0,0,0)
Title.TextScaled = true
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame

local Scrolling = Instance.new("ScrollingFrame")
Scrolling.Size = UDim2.new(1, -20, 0, 300)
Scrolling.Position = UDim2.new(0, 10, 0, 60)
Scrolling.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Scrolling.ScrollBarThickness = 8
Scrolling.Parent = MainFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 5)
UIListLayout.Parent = Scrolling

-- Boutons
local BtnTpTo = Instance.new("TextButton")
BtnTpTo.Size = UDim2.new(0.48, 0, 0, 50)
BtnTpTo.Position = UDim2.new(0, 10, 0, 370)
BtnTpTo.BackgroundColor3 = Color3.fromRGB(0, 102, 255)
BtnTpTo.Text = "Se téléporter à lui"
BtnTpTo.TextColor3 = Color3.new(1,1,1)
BtnTpTo.TextScaled = true
BtnTpTo.Font = Enum.Font.GothamSemibold
BtnTpTo.Parent = MainFrame

local BtnBring = Instance.new("TextButton")
BtnBring.Size = UDim2.new(0.48, 0, 0, 50)
BtnBring.Position = UDim2.new(0.5, 0, 0, 370)
BtnBring.BackgroundColor3 = Color3.fromRGB(255, 136, 0)
BtnBring.Text = "Il se téléporte à moi"
BtnBring.TextColor3 = Color3.new(1,1,1)
BtnBring.TextScaled = true
BtnBring.Font = Enum.Font.GothamSemibold
BtnBring.Parent = MainFrame

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(1, -20, 0, 40)
CloseBtn.Position = UDim2.new(0, 10, 0, 430)
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
CloseBtn.Text = "Fermer"
CloseBtn.TextColor3 = Color3.new(1,1,1)
CloseBtn.TextScaled = true
CloseBtn.Parent = MainFrame

-- Fonction pour mettre à jour la liste
local function UpdatePlayerList()
    for _, child in pairs(Scrolling:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local PlayerBtn = Instance.new("TextButton")
            PlayerBtn.Size = UDim2.new(1, -10, 0, 50)
            PlayerBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
            PlayerBtn.Text = player.Name .. " (" .. player.UserId .. ")"
            PlayerBtn.TextColor3 = Color3.new(1,1,1)
            PlayerBtn.TextScaled = true
            PlayerBtn.Font = Enum.Font.Gotham
            PlayerBtn.Parent = Scrolling
            
            PlayerBtn.MouseButton1Click:Connect(function()
                -- Désélectionner tous
                for _, b in pairs(Scrolling:GetChildren()) do
                    if b:IsA("TextButton") then
                        b.BackgroundColor3 = Color3.fromRGB(50,50,50)
                    end
                end
                PlayerBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 136)
                selectedPlayer = player
            end)
        end
    end
    Scrolling.CanvasSize = UDim2.new(0,0,0, UIListLayout.AbsoluteContentSize.Y)
end

-- Mise à jour automatique
Players.PlayerAdded:Connect(UpdatePlayerList)
Players.PlayerRemoving:Connect(UpdatePlayerList)
UpdatePlayerList()

-- Boutons actions
BtnTpTo.MouseButton1Click:Connect(function()
    if selectedPlayer and selectedPlayer.Character and selectedPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = selectedPlayer.Character.HumanoidRootPart.CFrame + Vector3.new(0, 3, 0)
        end
    end
end)

BtnBring.MouseButton1Click:Connect(function()
    if selectedPlayer and selectedPlayer.Character and selectedPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local myHrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if myHrp then
            selectedPlayer.Character.HumanoidRootPart.CFrame = myHrp.CFrame + Vector3.new(0, 3, 0)
        end
    end
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

print("Panel TP chargé ! Clique sur un joueur puis sur les boutons.")
